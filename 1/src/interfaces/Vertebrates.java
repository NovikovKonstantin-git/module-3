package interfaces;

public interface Vertebrates {
    String vertebra(String has_vertabra); // есть позвоночник? Если нет, по каким причинам его нет
    boolean protect(boolean number); // выполняет ли позвоночник функцию защиты?
}
